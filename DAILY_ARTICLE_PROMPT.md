# دستور کار مقاله روزانه (ایمپلنت + ارتودنسی)

هر بار که این کار اجرا می‌شود:

1. یک عنوان آموزشی فارسی درباره **ایمپلنت** یا **ارتودنسی** بنویس (تمرکز اصلی این دو حوزه).
2. از مقالات پربازدید ایرانی ایده بگیر؛ محتوا را **بازنویسی** کن و منبع را با لینک ذکر کن (کپی لفظی ممنوع).
3. در متن اشاره کن به: دکتر پوریا کمانگر، کلینیک دندانپزشکی سلامت، بندر ماهشهر، خیابان منتظری، اینستاگرام `@dr.pourya.kamangar`، پذیرش۲۴ و پروفایل‌های نوبت‌دهی.
4. مقاله را به ابتدای آرایه `dental-clinicsite/articles.json` اضافه کن با فیلدهای: `id`, `date` (امروز UTC+2 یا تاریخ محلی), `title`, `summary`, `tags`, `body` (۴ پاراگراف), `sources`.
5. `id` یکتا و انگلیسی-kebab باشد.
6. بعد از ذخیره: commit و push به `origin/main` در ریپوی dental-clinicsite.

نمونه تگ‌ها: `["ایمپلنت","آموزشی"]` یا `["ارتودنسی","آموزشی","بندر ماهشهر"]`
